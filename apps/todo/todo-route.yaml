apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: todo
    cleanup.argocd/example: sync-waves
  name: todo
  namespace: sync-waves
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  port:
    targetPort: 8080
  to:
    kind: Service
    name: todo-gitops
    weight: 100
